# 家庭书架管理系统 - 规格文档

## 1. 项目概述

- **项目名称**: 家庭书架管理系统
- **项目类型**: Web应用 (Python Flask + HTML/CSS/JS)
- **核心功能**: 管理家庭书架，支持层数/格子配置、图书借还管理
- **目标用户**: 家庭成员

## 2. 功能规格

### 2.1 书架配置
- 可设置书架总层数（默认3层）
- 每层可设置格子数量（每层独立设置）
- 各层总宽度相同，每格可单独设置宽度比例
- 书架配置持久化存储（JSON文件）

### 2.2 图书管理
- 每格可添加多本书
- 支持添加、删除图书
- 支持借出、归还操作
- 每格显示当前所有书名

### 2.3 搜索功能
- 支持按书名搜索
- 显示图书所在的层和格位置

### 2.4 数据结构
```json
{
  "layers": [
    {
      "shelf_width": 100,
      "cells": [
        {"width": 30, "books": [{"name": "书名", "borrowed": false}]},
        {"width": 70, "books": []}
      ]
    }
  ]
}
```

## 3. 视觉规格

### 3.1 布局
- 顶部: 标题 + 搜索栏
- 中部: 书架可视化展示（从上到下，每层横向排列格子）
- 底部: 配置按钮

### 3.2 书架展示
- 每层用不同颜色区分
- 每格显示格子内所有书名
- 借出状态用不同颜色标识（如红色边框/斜体）
- 格子宽度按比例显示

### 3.3 交互
- 点击格子: 弹出操作菜单（添加图书/借出/归还/删除）
- 点击配置按钮: 弹出书架配置弹窗
- 搜索框实时过滤

### 3.4 配色
- 主色: #8B4513 (书架木色)
- 背景: #F5F5DC (米色)
- 借出状态: #CC0000 (红色)
- 可用状态: #006400 (绿色)

## 4. 技术实现

- **后端**: Python Flask
- **前端**: 原生HTML/CSS/JavaScript
- **数据存储**: JSON文件
- **端口**: 5000

## 5. 验收标准

1. ✅ 可以设置书架层数
2. ✅ 可以设置每层格子数
3. ✅ 各层宽度相同，格子可单独设置宽度比例
4. ✅ 可以添加、删除图书
5. ✅ 可以标记借出、归还
6. ✅ 每格显示当前所有书名
7. ✅ 支持搜索，显示图书位置
8. ✅ 数据持久化